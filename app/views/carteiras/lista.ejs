<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <table>
            <thead>
                <th>Ticker</th>
                <th>Empresa</th>
                <th>Setor</th>
                <th>Preço</th>
                <th>Qtd</th>
                <th>Custo</th>
                <th>Valor de Mercado</th>
                <th>Ganho</th>
                <th>Retorno</th>
                <th>Peso</th>
            </thead>
            <tbody>
                    <% for(var i = 0; i < lista.length; i++){ %>
                    <tr>
                        <td><%= lista[i].ticker %></td>
                        <td><%= lista[i].name %></td>
                        <td><%= lista[i].sector %></td>
                        <td><%= lista[i].price %></td>
                        <td><%= lista[i].quantity %></td>
                        <td><%= lista[i].cost_basis %></td>
                        <td><%= lista[i].mkt_value %></td>
                        <td><%= lista[i].gain_cash %></td>
                        <td><%= lista[i].overall_return %></td>
                        <td><%= lista[i].weight %></td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </body>
</html>
